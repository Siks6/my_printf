#ifndef MAIN_H_
#define MAIN_H_

#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
#include <math.h>
int _putchar(char c);
int _printf(const char *format, ...);
int _puts(char *s);
char *my_itoa(int num, char *strg);

#endif
